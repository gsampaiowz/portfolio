import { Poppins } from "next/font/google";

export const poppinsExtraLight = Poppins({ weight: "200", subsets: ["latin"], variable: "--extraLight" });
export const poppinsRegular = Poppins({ weight: "400", subsets: ["latin"], variable: "--regular" });